{
  "name": "rym-visual-rating-bar",
  "private": true,
  "scripts": {
    "dev": "bun run bun.dev.ts",
    "build": "bun run bun.build.ts",
    "serve": "bun run bun.server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun": "^1.2.4",
    "bun-types": "^1.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}